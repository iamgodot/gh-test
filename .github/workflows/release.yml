name: release

on:
  push:
    branches:
      - master
    tags:
      - v[0-9].[0-9]+.[0-9]+
  release:
    types: [published]

jobs:
  deploy:

    runs-on: ubuntu-latest

    steps:
      - name: Release
        run: echo 'Release done.'
